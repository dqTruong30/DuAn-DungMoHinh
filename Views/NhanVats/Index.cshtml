@model IEnumerable<DungMoHinh.Models.NhanVat>

@{
    ViewData["Title"] = "Nhân vật";
}

<div class="page-heading">
    <div>
        <span class="eyebrow">Thư viện cá nhân</span>
        <h1>Nhân vật</h1>
        <p>Quản lý nhân vật đã tạo, mở chi tiết, chỉnh sửa thông tin hoặc xóa nhân vật khỏi hệ thống.</p>
    </div>
    <div class="toolbar">
        <a class="btn btn-primary" asp-action="Create">Tạo nhân vật</a>
    </div>
</div>

@if (!Model.Any())
{
    <div class="empty-state">
        <h2>Chưa có nhân vật nào</h2>
        <p>Hãy tạo nhân vật đầu tiên để bắt đầu xây dựng thư viện thiết kế 3D của bạn.</p>
        <a class="btn btn-primary" asp-action="Create">Tạo nhân vật</a>
    </div>
}
else
{
    <div class="table-wrap">
        <table class="table align-middle">
            <thead>
                <tr>
                    <th>Tên</th>
                    <th>Loại</th>
                    <th>Phong cách</th>
                    <th>Cập nhật</th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <strong>@item.Name</strong>
                        <div class="text-muted small">@(string.IsNullOrWhiteSpace(item.MoTa) ? "Chưa có mô tả" : item.MoTa)</div>
                    </td>
                    <td><span class="status-pill">@item.Kind.GetDisplayName()</span></td>
                    <td>@item.Style.GetDisplayName()</td>
                    <td>@(item.UpdatedAt ?? item.CreatedAt)</td>
                    <td class="text-end">
                        <div class="toolbar justify-content-end">
                            <a class="btn btn-sm btn-outline-secondary" asp-action="Details" asp-route-id="@item.Id">Chi tiết</a>
                            <a class="btn btn-sm btn-primary" asp-action="Edit" asp-route-id="@item.Id">Sửa</a>
                            <a class="btn btn-sm btn-outline-danger" asp-action="Delete" asp-route-id="@item.Id">Xóa</a>
                        </div>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    </div>
}
